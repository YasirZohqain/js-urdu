const accountId = 123456
let userName = "Yasir"
var email = "yasir@hotamil.com"

cityName = "karachi" // its work but good practices

userName = 'ok' 

let country // declare varible but assign value is always undefined

/* 
prefer not to use var
because of block scope {} and function scope {}

if (condition) {
    
}

*/

// accountId = 5654 const not allow to change value



console.table([accountId, userName, email, cityName, country])